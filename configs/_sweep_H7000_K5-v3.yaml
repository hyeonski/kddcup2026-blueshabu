dataset:
  root_path: data/public/input

agent:
  # Model access priority: env > YAML > default.
  # During official evaluation, MODEL_API_URL / MODEL_API_KEY / MODEL_NAME
  # are injected by the grader and override these fields automatically.
  # Locally, prefer setting env vars instead of editing this file. Never commit real keys.
  model: MODEL
  api_base: API_BASE_URL
  api_key: API_KEY
  max_steps: 100
  temperature: 1.0
  # ACON Context Optimization
  enable_context_optimization: true
  history_summarization_threshold: 7000
  preserve_last_k_steps: 5

run:
  output_dir: artifacts/runs
  # run_id 규칙:
  #   - 비워두거나 생략하면 모델+파라미터로 자동 생성 (예: qwen3.5-35b-a3b-T1.0-H15000-K5)
  #   - "{params}" 플레이스홀더가 있으면 해당 위치에 T/H/K 등 핵심 파라미터를 채워넣음
  #     예: "exp-{params}-r2" → "exp-T1.0-H15000-K5-r2"
  #   - 그 외 문자열은 그대로 사용 (legacy)
  # 어느 경우든 summary.json의 agent_config 필드에 전체 파라미터 스냅샷이 저장됨.
  run_id: "ACON-2605110507-T1.0-H7000-K5-v3"
  max_workers: 4
  task_timeout_seconds: 900
